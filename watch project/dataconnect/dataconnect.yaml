specVersion: "v1"
serviceId: "watchproject"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "watchproject-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
